# Morning Briefing — 2026-05-20

**Branch:** `claude/loving-sagan-tiYFt`  
**PR:** [#85 — refactor(hub): Phase 5 hub.js decomposition](https://github.com/joelhmartin/Anchor-Client-Dashboard/pull/85) ← Ready for Review  
**Run cost:** ~$1.40 | **Duration:** ~60 min

---

## What Shipped Last Night

4 units of hub.js decomposition (Phase 5):

| Commit | What |
|--------|------|
| `a30f05f` | New: `server/services/hubUtils.js` — shared `logEvent` + `resolveBaseUrl` utilities |
| `71111ae` | New: `server/routes/blogPosts.js` — 8 blog routes + 2 private helpers (~427 LOC) |
| `3aaa4d4` | New: `server/routes/twilioConfig.js` — 9 Twilio config routes (~305 LOC) |
| `13e425e` | New: `server/routes/onboardingPdfRoutes.js` — 2 PDF regen routes (~110 LOC) |
| `d7b9262` | State + log update |

**hub.js: 12,657 → 11,833 lines (-824 lines)**

All compliance gates green. All `node --check` passed.

---

## Decisions Needed From You

These items are **blocked** and cannot progress without your call:

1. **`formatUserName` email fallback** — `formatUserName(row)` falls through to `row.email` when first/last name is blank. The originals in `auth.js`, `notifications.js`, `onboardingPdf.js`, and `templateVariables.js` produce `'there'` or empty string instead. **Is the email fallback acceptable in salutations and PDFs?**

2. **`isPlaceholderEmail` guard in `AdminHub.jsx:636` / `ClientView.jsx:93`** — pattern `isPlaceholderEmail(c.email) ? 'New Client' : c.email` has no equivalent in `clientLabel()`. **Should `clientLabel()` be extended with a placeholder-email guard, or leave these sites inline?**

---

## Next Best Work (No Human Required)

1. **Extract Client Team Management routes** — ~840 lines (hub.js lines 10993–11833). Complex: invites, tokens, email sends. Do import audit first — `hashToken`, `maskEmail`, `sendMailgunMessage`, `sendMailgunMessageWithLogging`, `createNotification`, `notifyAdminsByEmail`, `logUserActivity`, `logClientActivity`, `logDocumentActivity`, `fetchActivityLogs`, `ActivityEventTypes`, `ActivityCategories`, `logSecurityEvent`, `SecurityEventTypes`, `SecurityEventCategories`, `encrypt`, `decrypt`, `isEncrypted`. Most of these are used elsewhere in hub.js so imports stay in hub.js; only the route block moves.

2. **Extract OAuth Providers CRUD** — ~90 lines (hub.js lines 9529–9620). Smallest clean OAuth chunk; good warmup before the full OAuth section.

3. **`ctmForms.js` split** — Templates section (lines 1491–1729, ~238 LOC) is the cleanest seam if you want a non-hub target.

